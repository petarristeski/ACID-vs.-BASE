# IoT Scenario

- Ops: sensor writes, time-series queries
- Characteristics: high write throughput, rare updates
- Tests: high-throughput writes, time-range queries, retention

See schemas in `databases/*/iot.*`.
